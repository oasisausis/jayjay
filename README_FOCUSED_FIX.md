# Focused dashboard update

This update preserves the existing dark dashboard design.

## Changes
1. Removed the homepage right-side explanatory paragraph and Today’s Equation strip.
2. Increased card-title weight, size and contrast; strengthened descriptions, labels and action buttons.
3. Repaired the collapsed onboarding-pass panel on `transport.html` by restoring the missing full-width grid rule and responsive layout.

## Replace
- `index.html`
- `styles.css`

`transport.html` is included unchanged for convenience; the panel repair is in `styles.css`.
